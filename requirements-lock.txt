# Pinned, reproducible versions tested on the reference machine (Python 3.10.19, RTX A6000, CUDA).
# `requirements.txt` stays unpinned for readability; use this file for an exact-version install.
#
# NOTE: torch / torchvision are CUDA-driver specific. Install a CUDA-matched PyTorch build FIRST
# (the pins below only record the tested versions), then: pip install -r requirements-lock.txt
numpy==1.26.4
Pillow==12.0.0
opencv-python==4.11.0.86
einops==0.8.1
safetensors==0.7.0
torch==2.9.1
torchvision==0.24.1
trimesh==4.10.1
matplotlib==3.10.7
scipy==1.15.3
open3d==0.19.0
transformers==5.9.0
